let myName = "Kinsey";
let currentAge = 25;
let currentCity = "Indianapolis";
console.log(`Hi, I'm ${myName} and I am currently ${currentAge}. I live in ${currentCity}.`);

function hello(first, last) {
    let wholeName = first + " " + last;
    return wholeName;
}
    console.log(hello("Kinsey", "Johnson"));

